#!/usr/bin/python

import sys
import sqlite3

def getRecommendedPlaces(uid, geo_info):
    return [1, 2]
#    conn = sqlite3.connect('traceshare_test.db')
#    c = conn.cursor()

    # 1. get all places
    # 2. get user's places
    # 1 - 2

#    conn.close()
